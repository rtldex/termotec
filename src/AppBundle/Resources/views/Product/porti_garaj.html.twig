{% extends 'AppBundle::layout.html.twig' %}
{% block content %}
    <h1 class="text-center">Poarti de garaj</h1>
    <div class="spacer30"></div>
    <div class="container">
        <div class="row" id="row">
            {% for product in products %}
                {% include 'AppBundle:Product:product.html.twig' with {entity: product} %}
            {% endfor %}
        </div>
    </div>
{% endblock %}